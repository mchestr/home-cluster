---
- name: Update octoprint
  hosts: octoprint
  gather_facts: true
  any_errors_fatal: true
  tags: octoprint
  become: true

  tasks:
    - name: Update
      ansible.builtin.import_role:
        name: update.octoprint
      tags: update
