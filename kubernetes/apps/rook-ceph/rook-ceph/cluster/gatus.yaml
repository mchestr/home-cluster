---
apiVersion: v1
kind: ConfigMap
metadata:
  name: rook-ceph-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: rook-ceph
        group: internal
        interval: 1h
        url: https://rook.chestr.dev
        conditions:
          - "[STATUS] == 200"
          - "[BODY] != pat(*auth.chestr.dev*)"
        alerts:
          - type: pushover
      - name: rook-ceph
        group: guarded
        interval: 1h
        url: 1.1.1.1
        dns:
          query-name: rook.chestr.dev
          query-type: A
        conditions:
          - "len([BODY]) == 0"
        alerts:
          - type: pushover
