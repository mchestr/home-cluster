---
apiVersion: v1
kind: ConfigMap
metadata:
  name: tautulli-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: tautulli
        group: internal
        interval: 1h
        url: https://tautulli.chestr.dev
        conditions:
          - "[STATUS] == 200"
          - "[BODY] != pat(*auth.chestr.dev*)"
        alerts:
          - type: pushover
      - name: tautulli
        group: guarded
        interval: 1h
        url: 1.1.1.1
        dns:
          query-name: tautulli.chestr.dev
          query-type: A
        conditions:
          - "len([BODY]) == 0"
        alerts:
          - type: pushover
