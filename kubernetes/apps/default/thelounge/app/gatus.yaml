---
apiVersion: v1
kind: ConfigMap
metadata:
  name: thelounge-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: thelounge
        group: guarded
        interval: 1h
        url: 1.1.1.1
        dns:
          query-name: thelounge.chestr.dev
          query-type: A
        conditions:
          - "len([BODY]) == 0"
        alerts:
          - type: pushover
