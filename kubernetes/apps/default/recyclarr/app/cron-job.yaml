---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: &app recyclarr
  namespace: default
spec:
  schedule: "@daily"
  jobTemplate:
    spec:
      ttlSecondsAfterFinished: 86400
      template:
        spec:
          automountServiceAccountToken: false
          restartPolicy: OnFailure
          initContainers:
            - name: init-config
              image: ghcr.io/onedr0p/alpine:3.18.0
              envFrom:
                - secretRef:
                    name: *app
              command: ["/bin/bash", "-c"]
              args: ["envsubst < /config/recyclarr.yml > /shared/recyclarr.yml"]
              volumeMounts:
                - name: config
                  mountPath: /config
                - name: shared
                  mountPath: /shared
          containers:
            - name: main
              image: ghcr.io/recyclarr/recyclarr:4.4.1
              command: ["/app/recyclarr/recyclarr"]
              args: ["sync"]
              volumeMounts:
                - name: shared
                  mountPath: /config/recyclarr.yml
                  subPath: recyclarr.yml
                  readOnly: true
          volumes:
            - name: config
              configMap:
                name: *app
            - name: shared
              emptyDir: {}
