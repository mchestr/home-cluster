---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: &app cloudnative-pg
  namespace: default
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: cloudnative-pg-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      metadata:
        labels:
          cnpg.io/reload: "true"
      data:
        username: "{{ .POSTGRES_SUPER_USER }}"
        password: "{{ .POSTGRES_SUPER_PASS }}"
        s3-access-key-id: "{{ .S3_POSTGRESQL_ACCESS_KEY }}"
        s3-secret-access-key: "{{ .S3_POSTGRESQL_SECRET_KEY }}"
        b2-access-key-id: "{{ .CLOUDNATIVE_PG_ACCESS_KEY_ID }}"
        b2-secret-access-key: "{{ .CLOUDNATIVE_PG_SECRET_ACCESS_KEY }}"
  dataFrom:
    - extract:
        key: cloudnative-pg
    - extract:
        key: minio
    - extract:
        key: backblaze
