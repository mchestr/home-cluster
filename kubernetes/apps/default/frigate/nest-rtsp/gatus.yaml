---
apiVersion: v1
kind: ConfigMap
metadata:
  name: nest-rtsp-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: nest-rtsp
        group: internal
        interval: 1h
        url: https://nest-rtsp.chestr.dev
        conditions:
          - "[STATUS] == 200"
          - "[BODY] == pat(*auth.chestr.dev*)"
        alerts:
          - type: pushover
      - name: nest-rtsp
        group: guarded
        interval: 1h
        url: 1.1.1.1
        dns:
          query-name: nest-rtsp.chestr.dev
          query-type: A
        conditions:
          - "len([BODY]) == 0"
        alerts:
          - type: pushover
