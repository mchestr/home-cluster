---
groups:
  - name: zigbee2mqtt
    rules:
      - alert: ZigbeeMQTTUnreachable
        expr: |
          sum(count_over_time({k8s_instance="zigbee2mqtt"} |~ "(?i)not connected to mqtt server"[2m])) > 0
        for: 2m
        labels:
          severity: critical
          category: logs
        annotations:
          app: "{{ $labels.k8s_instance }}"
          summary: "{{ $labels.k8s_instance }} is unable to reach MQTT"
