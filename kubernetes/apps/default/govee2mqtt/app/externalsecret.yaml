---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: &app govee2mqtt
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword
  target:
    name: govee2mqtt-secret
    creationPolicy: Owner
    template:
      engineVersion: v2
      data:
        GOVEE_API_KEY: "{{ .GOVEE_API_KEY }}"
        GOVEE_MQTT_USER: "{{ .MOSQUITTO_MQTT_USERNAME }}"
        GOVEE_MQTT_PASSWORD: "{{ .MOSQUITTO_MQTT_PASSWORD }}"
  dataFrom:
    - extract:
        key: mosquitto
    - extract:
        key: govee2mqtt
