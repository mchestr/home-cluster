---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kromgo-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: kromgo
        group: external
        url: https://kromgo.chestr.dev
        interval: 30m
        client:
          dns-resolver: tcp://1.1.1.1:53
        conditions:
          - "[STATUS] == 404"
        alerts:
          - type: pushover
