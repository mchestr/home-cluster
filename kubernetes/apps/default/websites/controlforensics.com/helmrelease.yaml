---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app controlforensics
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
  maxHistory: 2
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    defaultPodOptions:
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: *app

    controllers:
      controlforensics:
        replicas: 3
        strategy: RollingUpdate
        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          app:
            image:
              repository: ghcr.io/mchestr/control-forensics
              tag: main@sha256:62580fae6f25eefe3a860df04971b0c1ad1dd497d0a7160afbfb52fa9ce9a48b
            envFrom:
              - secretRef:
                  name: controlforensics-secret
            resources:
              requests:
                cpu: 100m
                memory: 100Mi
              limits:
                memory: 500Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL

    service:
      app:
        controller: controlforensics
        ports:
          http:
            port: &port 3000

    route:
      app:
        hostnames:
          - www.controlforensics.com
        parentRefs:
          - name: external
            namespace: networking
            sectionName: controlforensics-https
        rules:
          - backendRefs:
              - name: *app
                port: *port
      tld:
        hostnames:
          - controlforensics.com
        parentRefs:
          - name: external
            namespace: networking
            sectionName: controlforensics-https-tld
        rules:
          - backendRefs:
              - name: *app
                port: *port
