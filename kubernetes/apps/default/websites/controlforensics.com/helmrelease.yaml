---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app controlforensics
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
  maxHistory: 2
  install:
    remediation:
      retries: -1
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    defaultPodOptions:
      topologySpreadConstraints:
        - maxSkew: 1
          topologyKey: kubernetes.io/hostname
          whenUnsatisfiable: DoNotSchedule
          labelSelector:
            matchLabels:
              app.kubernetes.io/name: *app

    controllers:
      controlforensics:
        replicas: 3
        strategy: RollingUpdate
        annotations:
          reloader.stakater.com/auto: "true"

        containers:
          app:
            image:
              repository: ghcr.io/mchestr/control-forensics
              tag: main@sha256:eee8bbafa5f5a3d4f0fa6669010d50d2e470b21d68ae0807acb706163822d4a8
            envFrom:
              - secretRef:
                  name: controlforensics-secret
            resources:
              requests:
                cpu: 100m
                memory: 100Mi
              limits:
                memory: 500Mi
            securityContext:
              allowPrivilegeEscalation: false
              readOnlyRootFilesystem: true
              capabilities:
                drop:
                  - ALL

    service:
      app:
        controller: controlforensics
        ports:
          http:
            port: &port 3000

    route:
      app:
        hostnames:
          - www.controlforensics.com
        parentRefs:
          - name: external
            namespace: networking
            sectionName: controlforensics-https
        rules:
          - backendRefs:
              - name: *app
                port: *port
      tld:
        hostnames:
          - controlforensics.com
        parentRefs:
          - name: external
            namespace: networking
            sectionName: controlforensics-https-tld
        rules:
          - backendRefs:
              - name: *app
                port: *port
