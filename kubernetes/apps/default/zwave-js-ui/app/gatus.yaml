---
apiVersion: v1
kind: ConfigMap
metadata:
  name: zwave-js-ui-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: zwave-js-ui
        interval: 1h
        url: https://zwave-js-ui.chestr.dev
        group: internal
        conditions:
          - "[STATUS] == 200"
          - "[BODY] == pat(*auth.chestr.dev*)"
        alerts:
          - type: pushover
      - name: zwave-js-ui
        interval: 1h
        url: 1.1.1.1
        group: guarded
        dns:
          query-name: zwave-js-ui.chestr.dev
          query-type: A
        conditions:
          - "len([BODY]) == 0"
        alerts:
          - type: pushover
