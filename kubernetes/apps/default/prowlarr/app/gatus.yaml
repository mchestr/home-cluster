---
apiVersion: v1
kind: ConfigMap
metadata:
  name: prowlarr-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: prowlarr
        group: internal
        interval: 1h
        url: https://prowlarr.chestr.dev
        conditions:
          - "[STATUS] == 200"
          - "[BODY] == pat(*auth.chestr.dev*)"
        alerts:
          - type: pushover
      - name: prowlarr
        group: guarded
        interval: 1h
        url: 1.1.1.1
        dns:
          query-name: prowlarr.chestr.dev
          query-type: A
        conditions:
          - "len([BODY]) == 0"
        alerts:
          - type: pushover
