---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - backups
  - external-secret.yaml
  - helm-release.yaml
  - pvc.yaml
configMapGenerator:
  - name: radarr-4k-pushover
    files:
      - ./scripts/pushover-notify.sh
generatorOptions:
  disableNameSuffixHash: true
  annotations:
    kustomize.toolkit.fluxcd.io/substitute: disabled
