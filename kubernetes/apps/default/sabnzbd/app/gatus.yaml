---
apiVersion: v1
kind: ConfigMap
metadata:
  name: shlink-api-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: shlink-api
        group: internal
        interval: 1h
        url: https://ln.chestr.dev
        conditions:
          - "[STATUS] == 200"
          - "[BODY] == pat(*auth.chestr.dev*)"
        alerts:
          - type: pushover
      - name: sabnzbd
        group: guarded
        interval: 1h
        url: 1.1.1.1
        dns:
          query-name: sabnzbd.chestr.dev
          query-type: A
        conditions:
          - "len([BODY]) == 0"
        alerts:
          - type: pushover
