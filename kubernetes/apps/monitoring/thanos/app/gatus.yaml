---
apiVersion: v1
kind: ConfigMap
metadata:
  name: thanos-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: thanos
        group: internal
        interval: 1h
        url: https://thanos.chestr.dev
        conditions:
          - "[STATUS] == 200"
          - "[BODY] == pat(*auth.chestr.dev*)"
        alerts:
          - type: pushover
      - name: thanos
        group: guarded
        interval: 1h
        url: 1.1.1.1
        dns:
          query-name: thanos.chestr.dev
          query-type: A
        conditions:
          - "len([BODY]) == 0"
        alerts:
          - type: pushover
