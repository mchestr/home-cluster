apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: &app gatus
  namespace: monitoring
spec:
  interval: 5m
  chart:
    spec:
      chart: app-template
      version: 1.5.0
      interval: 15m
      sourceRef:
        kind: HelmRepository
        name: bjw-s-charts
        namespace: flux-system
  values:
    controller:
      replicas: 1
      annotations:
        reloader.stakater.com/auto: "true"

    initContainers:
      init-db:
        image: ghcr.io/onedr0p/postgres-initdb:14.7
        envFrom:
          - secretRef:
              name: *app

    image:
      repository: ghcr.io/twin/gatus
      tag: v5.4.0@sha256:04ef57b6e0a113b6d09f5d787efdd08e6d51f6cbc470cf1313d349a163568dc9

    env:
      TZ: "${TZ}"

    envFrom:
      - secretRef:
          name: *app

    persistence:
      config:
        enabled: true
        type: configMap
        name: gatus-configmap

    service:
      main:
        ports:
          http:
            port: 8080

    ingress:
      main:
        enabled: true
        annotations:
          hajimari.io/icon: mdi:connection
          hajimari.io/info: Service Status Monitor
          hajimari.io/group: system management
        hosts:
          - host: &host "{{ .Release.Name }}.${SECRET_CLUSTER_DOMAIN}"
            paths:
              - path: /
                pathType: Prefix
        tls:
          - hosts:
              - *host

    podAnnotations:
      reloader.stakater.com/auto: "true"
