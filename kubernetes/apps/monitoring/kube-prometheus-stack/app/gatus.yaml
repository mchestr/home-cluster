---
apiVersion: v1
kind: ConfigMap
metadata:
  name: kps-gatus-ep
  namespace: default
  labels:
    gatus.io/enabled: "true"
data:
  config.yaml: |
    endpoints:
      - name: prometheus
        group: internal
        interval: 1h
        url: https://prometheus.chestr.dev
        conditions:
          - "[STATUS] == 200"
          - "[BODY] == pat(*auth.chestr.dev*)"
        alerts:
          - type: pushover
      - name: prometheus
        group: guarded
        interval: 1h
        url: 1.1.1.1
        dns:
          query-name: prometheus.chestr.dev
          query-type: A
        conditions:
          - "len([BODY]) == 0"
        alerts:
          - type: pushover

      - name: alert-manager
        group: internal
        interval: 1h
        url: https://alert-manager.chestr.dev
        conditions:
          - "[STATUS] == 200"
          - "[BODY] == pat(*auth.chestr.dev*)"
        alerts:
          - type: pushover
      - name: alert-manager
        group: guarded
        interval: 1h
        url: 1.1.1.1
        dns:
          query-name: alert-manager.chestr.dev
          query-type: A
        conditions:
          - "len([BODY]) == 0"
        alerts:
          - type: pushover
