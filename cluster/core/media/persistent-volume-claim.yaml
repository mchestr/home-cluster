---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: media-pv-claim
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi  # Doesn't matter with NFS
  volumeName: media-pv
  storageClassName: nfs-csi
