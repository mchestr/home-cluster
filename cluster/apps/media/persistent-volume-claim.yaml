---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: media-pv-claim
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeName: media-pv
  storageClassName: nfs-csi
