---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-pvc
  namespace: database-system
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-csi
  volumeName: minio-pv
  resources:
    requests:
      storage: 1Mi
