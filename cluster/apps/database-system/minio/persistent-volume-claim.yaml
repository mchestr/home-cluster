---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-nfs
  namespace: database-system
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: nfs-csi
  volumeName: media-pv
  resources:
    requests:
      storage: 1Mi
