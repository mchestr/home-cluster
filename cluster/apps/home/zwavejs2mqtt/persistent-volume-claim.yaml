---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: code-zwavejs2mqtt-pv-claim
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeName: code-zwavejs2mqtt-pv
  storageClassName: nfs-csi
