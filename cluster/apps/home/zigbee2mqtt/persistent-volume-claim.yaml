---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: code-zigbee2mqtt-pv-claim
  namespace: home
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeName: code-zigbee2mqtt-pv
  storageClassName: nfs-csi
