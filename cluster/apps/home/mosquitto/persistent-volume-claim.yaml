---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: code-mosquitto-pv-claim
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeName: code-mosquitto-pv
  storageClassName: nfs-csi
