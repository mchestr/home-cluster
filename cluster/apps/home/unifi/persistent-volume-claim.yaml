---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: code-unifi-pv-claim
  namespace: home
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeName: code-unifi-pv
  storageClassName: nfs-csi
