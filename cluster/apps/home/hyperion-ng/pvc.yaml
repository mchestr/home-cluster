---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hyperion-ng-config-v1
  namespace: home
  labels:
    app.kubernetes.io/name: &name hyperion-ng
    app.kubernetes.io/instance: *name
    snapshot.home.arpa/enabled: "true"
spec:
  accessModes:
    - ReadWriteOnce

  resources:
    requests:
      storage: 1Gi

  storageClassName: longhorn
