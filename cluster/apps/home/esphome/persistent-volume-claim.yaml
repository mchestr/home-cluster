---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: code-esphome-pv-claim
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeName: code-esphome-pv
  storageClassName: nfs-csi
