---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: factorio-k2-pvc
  namespace: game-system
  labels:
    recurring-job-group.longhorn.io/backup: enabled
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: longhorn
