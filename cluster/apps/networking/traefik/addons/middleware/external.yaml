---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: external
  namespace: networking
spec:
  chain:
    middlewares:
      - name: cloudflare-only
      - name: no-auth-error-pages
