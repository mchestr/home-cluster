---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: loki-pv-claim
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 80Gi
  storageClassName: longhorn
