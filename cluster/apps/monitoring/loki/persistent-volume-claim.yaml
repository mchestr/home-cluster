---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: loki-pv-claim
  namespace: monitoring
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 40Gi
  storageClassName: longhorn
