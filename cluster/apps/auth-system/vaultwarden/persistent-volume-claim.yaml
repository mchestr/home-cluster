---
kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: code-vaultwarden-pvc
  namespace: auth-system
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  volumeName: code-vaultwarden-pv
  storageClassName: nfs-csi
